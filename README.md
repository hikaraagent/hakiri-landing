# hakiri-landing

static landing site for [hakiri](https://github.com/hakiriagent/hakiri) — ethereum mev forensics agent.

two pages, no build step:

- `index.html` — hero
- `docs.html` — documentation

deployed on vercel as a static site.

## local

```sh
npx serve .
```
